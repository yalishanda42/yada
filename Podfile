# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Date Me' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Date Me
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'  

  target 'Date MeTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
